{
  "name": "galaxydo",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "test-frontend": "jest tests/Galaxy.test.js",
    "dev-frontend": "vite",
    "dev-deno": "cd desktop && DEV=true deno run --allow-all --unstable main.ts",
    "dev-desktop": "vite build && cd desktop && DEV=true deno run --allow-all --unstable main.ts",
    "excalidraw-first": "cd excalidraw/src/packages/excalidraw && yarn build:umd",
    "excalidraw-second": "cd excalidraw/src/packages/excalidraw && yarn publish",
    "excalidraw-third": "cd excalidraw && git add . && git commit -m 'up' && git push",
    "release-first": "mv dist/.git dist_git_backup && vite build && mv dist_git_backup dist/.git && cd dist && git checkout main && git pull && git add . && git commit -m 'up' && git push",
    "release-second": "cd desktop && deno compile --no-check --allow-all --unstable --target x86_64-apple-darwin --output GalaxyBrowser main.ts",
    "release-third": "cd desktop && deno run --allow-all --unstable build.ts",
    "build": "vite build",
    "pull-submodules": "git -C contract checkout main && git -C contract pull origin main && git -C desktop checkout main && git -C desktop pull origin main && git -C desktop/deno-webui checkout main && git -C desktop/deno-webui pull origin main && git -C desktop/dynamic-import checkout main && git -C desktop/dynamic-import pull origin main && git -C desktop/installer checkout main && git -C desktop/installer pull origin main && git -C desktop/webui checkout main && git -C desktop/webui pull origin main && git -C dist checkout main && git -C dist pull origin main && git -C excalidraw checkout master && git -C excalidraw pull origin master"
  },
  "dependencies": {
    "@excalidraw/excalidraw": "npm:@galaxydo/excalidraw@latest",
    "@galaxydo/excalidraw": "latest",
    "@galaxydo/excalidraw-utils": "0.1.2-23.8.18",
    "@polkadot/api": "^10.5.1",
    "@polkadot/extension-dapp": "^0.46.1",
    "@polkadot/util": "^12.1.1",
    "@polkadot/util-crypto": "^12.1.1",
    "kubo-rpc-client": "^3.0.1",
    "lodash.debounce": "^4.0.8",
    "lodash.throttle": "^4.1.1",
    "million": "^2.3.0",
    "nanoid": "^5.0.1",
    "puppeteer": "^19.11.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "useink": "^1.14.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.22.10",
    "@babel/preset-typescript": "^7.22.11",
    "@jest/globals": "^29.6.2",
    "@types/jest": "^29.5.3",
    "@types/node": "^20.5.6",
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@typescript-eslint/eslint-plugin": "^5.57.1",
    "@typescript-eslint/parser": "^5.57.1",
    "@vitejs/plugin-react-swc": "^3.0.0",
    "babel-jest": "^29.6.4",
    "eslint": "^8.38.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.3.4",
    "jest": "^29.6.2",
    "jest-environment-jsdom": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.0.2",
    "vite": "^4.3.0"
  }
}
